---
name: project_press_interactions
description: Press-interaction UI coverage (issue #120) — key-preview-balloon identifier, off-main XCUITest query technique for transient press-scoped UI, cap-release commit test
metadata:
  type: project
---

Established 2026-07-04 (issue #120 work), all verified by three consecutive green runs on the iOS 26 simulator toolchain.

- **`key-preview-balloon` identifier exists kit-side** (`KeyPreviewBalloon` in `IPAKeyboardKit/UI/KeyboardView.swift`): a single *unlabeled* accessibility element (`.accessibilityElement(children: .ignore)` + identifier), present exactly while an insert key is pressed on iPhone (never on iPad — `KeyButton.showsPreviewBalloon`; balloon tests `XCTSkip` on non-phone idiom). Before #120 it was `.accessibilityHidden(true)`, which removes an element from the XCUITest tree entirely — a hidden element is NOT queryable, so making transient UI assertable requires replacing hidden(true) with an unlabeled element, not adding an identifier alongside it.
- **XCUIAutomation thread rules on this toolchain (empirical, load-bearing):** event *synthesis* (`press(forDuration:)` etc.) from a non-main thread raises `NSInternalInconsistencyException` "Must be called on the main thread" — the background-gesture-plus-main-thread-assert pattern from older blog lore is DEAD here. Element *queries* (`exists`) evaluate fine from a background thread. So mid-press assertion of transient UI = gesture on main (blocking) + background observer thread polling queries. Helper `observe(whilePerforming:poll:)` in AlternatesPopupUITests.swift: `OSAllocatedUnfairLock` state, 0.1 s poll cadence, stops at first `true`, always joins (expectation + `wait`) before returning so no orphaned queries survive into the next gesture/failure/teardown. 10 s hold (`pressHoldDuration`) gives issue-#96-scale poll-inflation margin.
- **Swift 6 wrinkle:** the SDK marks `XCUIElement` Sendable but its members `@MainActor`, so `.exists` inside a `@Sendable` closure is a warning (`nonisolated(unsafe)` on the element is flagged unnecessary). Warning-free off-main read: `existsOffMain(_:)` via KVC `value(forKey: "exists")` — same public ObjC getter, no isolation diagnostic.
- **Cap-release commit** (PR #107 behavior): a stationary element-targeted `press(forDuration: 0.8)` on a key *with* alternates releases on the cap's center → types the base symbol exactly once. Asserted in the editor scratchpad via a label-predicate-scoped query (`identifier == layout-editor-scratch AND label == "ɹ"` + `waitForExistence`) — the bare `scratch.label` snapshot right after the press can race the SwiftUI re-render. Exactly-once falls out of exact label equality ("ɹɹ" on double emission).
- The frame-settle loop from the slide-to-popup test is now the shared `waitForStableFrame(of:)` helper; `openEditorPreviewRhotic()` is the shared launch→detail→editor arrange used by both scratchpad tests. See [[project_uitest_baseline]] for the preview-key identifier scheme (`key-insert-<text>`).
